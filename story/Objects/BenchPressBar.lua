BenchPressBar = class(SampStoryObjectBase, function(o, params)
    params.description = "bench press bar"
    params.type = 'BenchPressBar'

    SampStoryObjectBase.init(o, params)
end
)

BenchPressBar.eModel = {
    BenchPressBar1 = 2913
}